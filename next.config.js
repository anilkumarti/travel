/** @type {import('next').NextConfig} */
const nextConfig = {
  // Only include valid Next.js options here
  reactStrictMode: true,
  // Remove swcMinify if causing issues (it's enabled by default in newer versions)
}

module.exports = nextConfig